worker_processes 1;
daemon off;

events { worker_connections 1024; }

http {
    server {
        server_name _;
        keepalive_timeout 5;
        location / {
            root dist;
        }
    }
}
